name: nabd_al_hayah
description: Nabd Al-Hayah - Your Personal Nutrition Companion 
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  google_fonts: ^6.0.0
  percent_indicator: ^4.2.2
  fl_chart: ^0.65.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # if you want to use a local logo put it here:
  assets:
    - assets/images/
